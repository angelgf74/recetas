<?xml version="1.0" encoding="utf-8"?>
<!--
    Sin tráfico en claro, salvo hacia el equipo de desarrollo.

    10.0.2.2 es como el emulador ve el "localhost" de la máquina anfitriona, y la
    API local corre sin TLS. Fuera de esa dirección, cleartextTrafficPermitted es
    false: la API de producción va por HTTPS y no hay motivo para admitir otra cosa.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
